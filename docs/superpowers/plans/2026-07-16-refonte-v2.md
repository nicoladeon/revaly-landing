# Refonte v2 revaly.io — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconstruire revaly.io selon `docs/superpowers/specs/2026-07-16-refonte-v2-design.md` : home 8 sections (angle « autonomes 24/24 », 6 blocs agents COMPLETS façon Limova, pile de valeur, pricing 97/229 + annuel) + un catalogue /integrations, générés depuis des JSON. PAS de pages par agent (correctif Julien 16/07).

**Architecture:** build.py devient un générateur statique : `templates/` (partials head/nav/footer + gabarits page) + `data/` (agents.json, integrations.json, faq.json) → `dist/` (index, integrations, robots.txt, sitemap.xml). Le CSS/JS existant de template.html (marquee, spotlight, compteurs, animation « CRM vivant », modal) est conservé et découpé en partials réutilisés par toutes les pages.

**Tech Stack:** HTML/CSS/JS vanilla (pas de framework), Python 3 stdlib pour le build (aucune dépendance pip), Remotion pour la vidéo hero, Cloudflare Pages.

## Global Constraints

- Copy : règles du spec §1 — chaque réponse d'agent contient un chiffre/lieu/heure ; jamais « limité à » ; jamais de nom de fournisseur IA ; micro-texte « 0 € aujourd'hui · rappel avant la fin de l'essai · annulation en 2 clics » sous CHAQUE CTA ; on ne cite pas d'outils dans les phrases de vente.
- Cartes agents : UNIQUEMENT les capacités de l'annexe A du spec (vérifiées prod 16/07). Interdits : « Emma relance toute seule », tout rapprochement fait par Revaly, toute capacité de Hugo (inactif).
- DA : Fraunces + Geist auto-hébergées (`assets/fonts/`), iris `#4F46E5`/var(--iris), light+dark, wordmark « Revaly. » — AUCUN changement de tokens.
- Motion : vanilla IO + CSS transform/opacity uniquement, 150-300 ms, ease-out entrée, stagger 30-50 ms, UN moment signé par section (spec §6), reduced-motion/no-JS = statique. Pas de lib JS ajoutée.
- Outillage design OBLIGATOIRE (demande Julien) : `ui-ux-pro-max` (recherches domaine avant chaque section visuelle), banque `21st.dev` (MCP `mcp__21st__search`) pour les références de patterns, Remotion pour la vidéo.
- Prix : 97/229/« Nous contacter », annuel 970/2 290 (« 2 mois offerts »), défaut Mensuel. Plus AUCUNE trace de 199/169/229-barré.
- Build : `python3 build.py` sans dépendance ; chaque page a `<title>`, description, canonical, OG, beacon CF analytics (token existant dans template.html).
- Deploy : JAMAIS par l'agent — geste Julien (`npx wrangler pages deploy dist --project-name revaly`).

---

### Task 0: Préparation design (skills imposés)

**Files:**
- Create: `docs/superpowers/plans/design-notes-v2.md`

**Interfaces:**
- Produces: motion tokens + références de patterns consultés par les tasks 3-11.

- [ ] **Step 1: Recherches ui-ux-pro-max** (résultats collés dans design-notes-v2.md)

```bash
cd ~/.claude/skills/ui-ux-pro-max
python3 scripts/search.py "landing hero social-proof pricing" --domain landing -n 5
python3 scripts/search.py "scroll reveal stagger counter" --domain gsap -n 5
python3 scripts/search.py "animation accessibility loading z-index" --domain ux -n 8
```
Attendu : patterns + snippets choreography (tier Standard). On les adapte en vanilla (contrainte globale : pas de lib).

- [ ] **Step 2: Références 21st.dev** via MCP (`mcp__21st__search`), une requête par moment signé : `"ai chat card typing animation"`, `"pricing toggle monthly yearly"`, `"animated value stack counter"`, `"logo marquee"`. Noter dans design-notes-v2.md ce qu'on emprunte (structure/timing), PAS le code React (site vanilla).

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/plans/design-notes-v2.md && git commit -m "docs: notes design v2 (ui-ux-pro-max + 21st)"
```

---

### Task 1: Générateur multi-pages (build.py v2) — sans changer le rendu actuel

**Files:**
- Create: `templates/` (déplacement du contenu de template.html découpé), `tests/test_build.py`
- Modify: `build.py`
- Garder `template.html` intact jusqu'à la task 3 (bascule).

**Interfaces:**
- Produces: `render_page(template_name: str, ctx: dict) -> str` dans build.py ; tokens `{{TITLE}} {{DESCRIPTION}} {{CANONICAL}} {{OG_TITLE}} {{OG_DESC}} {{BODY}}` dans `templates/_shell.html` ; `write_page(path: str, html: str)`.

- [ ] **Step 1: Écrire le test**

```python
# tests/test_build.py — exécuter avec: python3 tests/test_build.py
import pathlib, re, subprocess, sys
ROOT = pathlib.Path(__file__).resolve().parents[1]
subprocess.run([sys.executable, str(ROOT / "build.py")], check=True)
dist = ROOT / "dist"
def must(cond, msg):
    if not cond: print("FAIL:", msg); sys.exit(1)
idx = (dist / "index.html").read_text()
must(idx.startswith("<!doctype html>"), "doctype")
must("og:title" in idx and "rel=\"canonical\"" in idx, "metas home")
rob = (dist / "robots.txt").read_text()
must(len(rob) < 2048 and "Sitemap:" in rob, "robots.txt réel (<2KB, sitemap)")
must("<urlset" in (dist / "sitemap.xml").read_text(), "sitemap.xml")
print("OK")
```

- [ ] **Step 2: Vérifier qu'il échoue** — `python3 tests/test_build.py` → `FAIL: robots.txt réel` (le fichier n'existe pas encore : le fallback CF Pages renvoie du HTML).

- [ ] **Step 3: build.py v2 (squelette).** Conserver l'injection base64 (dict `repl`) et la copie d'assets, ajouter :

```python
def render(tpl: str, ctx: dict) -> str:
    html = (ROOT / "templates" / tpl).read_text()
    for k, v in ctx.items():
        html = html.replace("{{" + k + "}}", v)
    return html

def write_page(rel: str, body: str, *, title: str, desc: str, path: str):
    shell = render("_shell.html", {
        "TITLE": title, "DESCRIPTION": desc,
        "CANONICAL": f"https://revaly.io{path}",
        "OG_TITLE": title, "OG_DESC": desc, "BODY": body,
    })
    for token, value in repl.items():
        shell = shell.replace(token, value)
    out = ROOT / "dist" / rel
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(shell)

PAGES = []  # rempli par chaque write_page → sitemap
```
`templates/_shell.html` = head actuel de template.html (metas variabilisées) + `{{BODY}}` + nav/footer partials. Cette task : la home passe par `write_page("index.html", body_home, ...)` où `body_home` = le body actuel inchangé.
robots.txt et sitemap écrits par build.py :

```python
(ROOT / "dist" / "robots.txt").write_text(
    "User-agent: *\nAllow: /\n\nSitemap: https://revaly.io/sitemap.xml\n")
urls = "".join(f"<url><loc>https://revaly.io{p}</loc></url>" for p in PAGES)
(ROOT / "dist" / "sitemap.xml").write_text(
    '<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">' + urls + "</urlset>")
```

- [ ] **Step 4: Test vert** — `python3 tests/test_build.py` → `OK`, et diff visuel : ouvrir dist/index.html, page identique à la prod actuelle.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(build): générateur multi-pages, robots.txt réel, sitemap"`

---

### Task 2: data/agents.json + data/faq.json (contenu = spec, vérifié prod)

**Files:**
- Create: `data/agents.json`, `data/faq.json`, extension de `tests/test_build.py`

**Interfaces:**
- Produces: schéma agent consommé par les tasks 4 et 10 :

```json
{
  "slug": "emma", "name": "Emma", "role": "assistante admin",
  "question": "Emma, quoi de neuf ?",
  "reponse": "Trois emails importants ce matin, brouillons de réponse prêts. …",
  "capacites": ["…3 phares…"],
  "fiche": {
    "automatisations": [{"titre": "…", "pitch": "…", "source": "cron-…"}],
    "outils": [{"titre": "…", "pitch": "…", "source": "slug-tool"}]
  }
}
```

- [ ] **Step 1: Test** — ajouter à tests/test_build.py : charge `data/agents.json`, assert 6 slugs `["max","emma","christine","zoe","raphael","lucas"]`, chaque `reponse` matche `re.search(r"\d", reponse)` (règle chiffre), chaque entrée fiche a un champ `source` non vide (traçabilité annexe A). Vérifier FAIL (fichiers absents).
- [ ] **Step 2: Rédiger agents.json** — contenu : conversations et capacités du spec §3.2 VERBATIM ; fiches de poste depuis l'annexe A (chaque tool/cron marketable → {titre, pitch, source}), pitchs écrits en bénéfice (« Alerte DPE — un nouveau diagnostic tombe dans ton secteur : tu le sais avant tout le monde », source: cron-dpe-alerts). Aucune entrée sans slug source réel.
- [ ] **Step 3: Rédiger faq.json** — les 8 Q/R du spec §3.7, réponses complètes (3-5 phrases), champ `schema: true` pour l'export JSON-LD.
- [ ] **Step 4: Test vert puis commit** — `python3 tests/test_build.py` → OK ; `git add data tests && git commit -m "feat(data): agents.json (inventaire vérifié) + faq.json"`

---

### Task 3: Home — hero + nav + purge des sections mortes

**Files:**
- Modify: `templates/` (body home extrait de template.html), `templates/_nav.html`, `templates/_footer.html`

- [ ] **Step 1:** Consulter design-notes-v2.md (Task 0). Nouveau hero (copy spec §3.1 exacte) : kicker « Ton équipe d'agents IA · formée à l'immobilier » (geo conservé), H1 `Agent immobilier,<br/>recrute ton équipe.`, sous-titre « Six agents IA autonomes, formés à ton métier, qui travaillent dans tes outils 24 h/24. Toi, tu fais visiter et tu signes. », CTA « Essayer 7 jours » + micro-texte. Supprimer « Regagne 10 heures » du H1.
- [ ] **Step 2:** Nav : Équipe (#equipe) · Intégrations (/integrations/) · Tarifs (#tarif) · FAQ (#faq) + Connexion + CTA. Footer aligné.
- [ ] **Step 3:** Supprimer les sections #promesse #nuit #journee #automatisations #telephone #marketing #confiance et leur CSS/JS orphelin (grep classes avant suppression). Garder #modelo (déplacée task 6), #equipe (refaite task 4), #preuve (fusionnée task 7), #tarif (refait task 8), #faq, CTA final.
- [ ] **Step 4:** Bande logos : marquee existante + badge final « + 3 200 outils via connecteurs → /integrations/ », Modelo/Netty en premières positions.
- [ ] **Step 5:** Build + QA visuelle (1440/390, light/dark, reduced-motion) via `/browse` screenshots. Commit `feat(home): hero autonomie + nav multi-page + purge sections`.

---

### Task 4: Home — section Équipe (6 BLOCS COMPLETS façon home Limova)

**CORRECTIF Julien 16/07 : pas de pages agents — tout visible sur la home.**

**Files:**
- Modify: template home (section #equipe), JS partial motion, build.py (boucle agents.json → HTML des blocs)

**Interfaces:**
- Consumes: agents.json (Task 2) — y compris `fiche.automatisations` et `fiche.outils` (affichés ICI, plus aucune page dédiée).

- [ ] **Step 1:** Références : 21st « ai chat card » (Task 0) + mécanique existante `.cmd-result armed/play` (IO 0.55, typing dots, stagger) de la section #modelo — la RÉUTILISER (classes préfixées `tc-`).
- [ ] **Step 2:** Markup par BLOC (généré par build.py, layout alterné gauche/droite desktop, empilé mobile) : ① portrait + pastille « en ligne » + nom/rôle ② conversation jouée : bulle question → typing dots → bulle réponse (chiffres en `<strong>`) ③ capacités (checks SVG) ④ « Ses automatisations » : 4-6 puces titre+pitch depuis fiche.automatisations/outils (les plus vendeuses d'abord) ⑤ « Discuter avec Emma » (modal existante). AUCUN lien vers une sous-page agent.
- [ ] **Step 3:** Motion : IO par bloc (conversation jouée au scroll, puces en stagger 40 ms après la réponse), reduced-motion = tout statique. La page se rallonge : vérifier que chaque bloc se scanne en 2 s (titre+conversation) et que les puces restent secondaires visuellement.
- [ ] **Step 4:** Build + QA (6 blocs jouent au scroll, aucun chiffre inventé vs agents.json, comparaison côte à côte avec la section agents de limova.ai). Commit `feat(home): section équipe — 6 blocs complets vérifiés`.

---

### Task 5: Home — « Rien ne part sans toi » (storyboard 3 temps)

- [ ] **Step 1:** Section nouvelle après #equipe : titre « Rien ne part sans toi. », sous-titre « Ils travaillent 24 h/24. Ils publient et envoient uniquement quand tu valides. » (véracité : send-email-guard + automation-consent, annexe A).
- [ ] **Step 2:** Storyboard scroll-triggered UNE séquence : ① carte brouillon email ② notif WhatsApp « Brouillon prêt — j'envoie ? » ③ réponse « ok envoie » tapée ④ coche stroke-dasharray (mécanique existante). Reduced-motion : 3 vignettes statiques côte à côte.
- [ ] **Step 3:** Build + QA + commit `feat(home): section contrôle — storyboard approbation`.

---

### Task 6: Home — « Formés à ton métier » (CRM vivant + prospection DPE)

- [ ] **Step 1:** Déplacer la section #modelo existante (animation intacte) sous un nouveau titre « Formés à ton métier » ; copy « la saisie, c'est fini » (create-modelo-contact/search).
- [ ] **Step 2:** Bloc prospection DPE (copy spec §3.4 exacte : tous les nouveaux DPE, rayon agence, F/G loi Climat en bonus) + ligne DVF/dossiers/droit FR + ligne « Et pour tout le reste : 3 200+ outils connectés → ».
- [ ] **Step 3:** Build + QA + commit `feat(home): section métier — CRM vivant + radar DPE`.

---

### Task 7: Home — pile de valeur (« Recruter cette équipe, en vrai, ça coûte combien ? »)

- [ ] **Step 1:** Sourcer les tarifs marché (2-3 requêtes web : salaire assistante immobilière mi-temps, tarif CM freelance, tarif photographe immo) ; retenir les fourchettes CONSERVATRICES du spec §3.5, noter les sources dans le HTML en commentaire.
- [ ] **Step 2:** Markup : 6 lignes (poste → tarif marché → agent) qui apparaissent en stagger, compteur (mécanique compteurs existante) qui grimpe → « plus de 2 000 €/mois » barré → chute « Ton équipe : 97 €/mois. Formée au métier, jamais malade, disponible 24 h/24. » Tabular-nums partout.
- [ ] **Step 3:** Bande de preuve sous la pile : 250+ contacts · 900+ messages · 2 898 syncs Modelo/30 j + ligne « et accessoirement : ~10 h de paperasse en moins par semaine ». Emplacements témoignages/note fondateur en HTML commenté.
- [ ] **Step 4:** Build + QA + commit `feat(home): pile de valeur`.

---

### Task 8: Home — tarifs 97/229 + toggle annuel + modal

- [ ] **Step 1:** Supprimer l'ancien bloc prix (compteur 229→199, barrés, `.count-price`) et son JS.
- [ ] **Step 2:** Grille 3 plans (copy = spec §3.6 : lignes « inclus : 30 photos studio + 3 vidéos/mois », « Nouvelles capacités chaque semaine, incluses », les 6 agents partout, « à partir de 46 €/conseiller », Réseaux = Nous contacter → mailto support@revaly.io). Badge Recommandé sur Agence (pas d'overflow:hidden sur .plan — piège connu).
- [ ] **Step 3:** Toggle Mensuel/Annuel : boutons radio stylés, swap des montants en JS (`data-monthly`/`data-yearly`), badge « 2 mois offerts », défaut Mensuel, zéro layout shift (largeur fixe tabular-nums). Micro-texte version annuelle : « 0 € aujourd'hui · 970 € à la fin de l'essai, annoncé par email · annulation en 2 clics ».
- [ ] **Step 4:** Modal (fonction waitlist existante, inchangée côté API) : titres → « Réserve ton essai — on t'ouvre l'accès sous 24 h », prix mentionnés 97/229. Bloc garanties sous la grille.
- [ ] **Step 5:** Build + grep `199|169|229 €<` dans dist → uniquement les nouveaux usages légitimes de 229. QA + commit `feat(home): pricing 97/229 + annuel 2 mois offerts`.

---

### Task 9: Home — FAQ (schema.org) + CTA final

- [ ] **Step 1:** build.py génère les accordéons depuis faq.json + un bloc `<script type="application/ld+json">` FAQPage (questions `schema: true`). Ajouter aussi JSON-LD Organization + Product/Offer (97 et 229 EUR).
- [ ] **Step 2:** CTA final : « Ton équipe peut commencer ce soir. » + 6 visages cascade (existant) + CTA + micro-texte.
- [ ] **Step 3:** Valider le JSON-LD (https://validator.schema.org/ via /browse). Commit `feat(home): FAQ générée + schema.org + close`.

---

### Task 10: SUPPRIMÉE (correctif Julien 16/07 — pas de pages agents)

Le contenu fiche de poste est rendu dans les blocs home (Task 4). Aucun
`dist/agents/` ne doit exister ; sitemap = `/` et `/integrations/` uniquement.

---

### Task 11: Page /integrations (catalogue searchable)

- [ ] **Step 1:** `data/integrations.json` : ~100 connecteurs pertinents métier depuis le catalogue public Pipedream (nom, catégorie parmi [CRM, Email, Agenda, Réseaux sociaux, Stockage, Compta, Signature, Téléphonie, Autre], une ligne). Modelo/Netty en natifs (encadré profondeur, hors grille).
- [ ] **Step 2:** templates/integrations.html : header « Branché sur les outils qui font ton quotidien » + bandeau « 3 200+ outils via connecteurs », input recherche + filtres catégorie (JS vanilla ~30 lignes : filter sur data-name/data-cat), grille de tuiles. Test build : page existe, contient ≥80 tuiles.
- [ ] **Step 3:** QA (recherche « gmail » → 1 tuile), commit `feat(integrations): catalogue searchable`.

---

### Task 12: Vidéo hero « la journée en 15 s » (Remotion)

**Files:**
- Modify: `video/src/Hero.tsx`, `assets/hero.mp4`, `assets/hero-poster.jpg`

- [ ] **Step 1:** Remonter Hero.tsx : horloge Fraunces 7h→7h (24 h compressées), notifications des agents réparties jour ET nuit (contenus = agents.json, ex. 09:12 Emma brouillon prêt, 14:20 vocal WhatsApp Max, 18:00 post Zoé, 01:40 retouches Raphaël, 05:20 sync Modelo), fin sur les 6 portraits. 15 s boucle.
- [ ] **Step 2:** Rendre : `cd video && npx remotion render src/index.ts hero ../assets/hero.mp4 --codec h264 --crf 24 --scale 2` (chromium = headless shell Playwright, remotion.config.ts existant). Régénérer le poster (frame 07:00).
- [ ] **Step 3:** Fallback mobile/reduced : ticker statique de 4 notifications horodatées (HTML/CSS, pas d'image). Vérifier autoplay (relance play() JS existante) + IntersectionObserver play/pause conservé.
- [ ] **Step 4:** Build + QA + commit `feat(video): hero 24h en 15s`.

---

### Task 13: OG, metas finales, GEO

- [ ] **Step 1:** og-src.html : title OG « Agent immobilier, recrute ton équipe. » (sans « 10 heures »), re-render og.jpg via /browse screenshot 1200×630. Metas home : description spec (« Six agents IA autonomes… dès 97 €/mois »).
- [ ] **Step 2:** Vérifier beacon CF analytics présent sur TOUTES les pages générées (grep dist).
- [ ] **Step 3:** Documenter le geste Julien dans le récap final : dashboard Cloudflare → zone revaly.io → désactiver le blocage AI crawlers (le robots.txt géré injecte les Disallow IA par-dessus le nôtre). Après deploy : `curl -s https://revaly.io/robots.txt` doit faire <2 Ko sans `Disallow: /` pour ClaudeBot/GPTBot.
- [ ] **Step 4:** Commit `feat(seo): og v4 + metas + robots`.

---

### Task 14: QA finale (spec §8) et recette

- [ ] **Step 1:** Dérouler la recette du spec §8 avec `/qa` : 2 pages × (1440/390 × light/dark × reduced-motion), parcours home→6 blocs agents→CTA, recherche integrations, ancres nav, partage OG WhatsApp, validator schema. Vérifier qu'AUCUN dist/agents/ n'existe.
- [ ] **Step 2:** Test d'œil : screenshots de chaque section de la home côte à côte avec limova.ai — retravailler toute section moins finie AVANT de conclure.
- [ ] **Step 3:** `python3 tests/test_build.py` vert + grep interdits : `grep -rE "199 ?€|169 ?€|pendant que tu dors|Regagne 10 heures" dist/` → zéro résultat.
- [ ] **Step 4:** Commit final + récap pour Julien : deploy (`!` wrangler) + geste Cloudflare AI crawlers + vérifs post-deploy (OG, /robots.txt, /integrations/).

---

## Self-review (fait à la rédaction)

- Couverture spec : §2 arborescence→T1/T10/T11 ; §3.1→T3 ; §3.2→T2/T4 ; §3.3→T5 ; §3.4→T6 ; §3.5→T7 ; §3.6→T8 ; §3.7→T9 ; §3.8→T9 ; §4 supprimé (blocs home) ; §5→T11 ; §6→T0+chaque task QA ; §7→T1/T9/T13 ; §8→T14 ; annexe A→T2. Vidéo §3.1→T12.
- Aucun placeholder « TBD » ; les contenus longs (copy cartes, FAQ, annexe A) sont référencés VERBATIM au spec qui vit dans le même repo — source unique, pas de duplication divergente.
- Cohérence types : `render/write_page` (T1) utilisés par T9/T10/T11 ; schéma agents.json (T2) consommé par T4/T10/T12.
